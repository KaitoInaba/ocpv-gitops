apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: networkgitops
  namespace: openshift-gitops
spec:
  destination:
    namespace: deploynw
    server: https://kubernetes.default.svc
  project: default
  source:
    path: deploynw/resources/
    repoURL: https://github.com/KaitoInaba/ocpv-gitops
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
    - CreateNamespace=true